spring:
  application:
    name: account-service

server:
  port: 8081

# Actuator: Health, Info & Prometheus nach aussen freigeben (Rest bleibt zu = sicher)
management:
  endpoints:
    web:
      exposure:
        include: health, info, prometheus
  info:
    env:
      enabled: true

info:
  app:
    name: account-service
    description: Accounts & double-entry ledger
